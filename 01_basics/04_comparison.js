// console.log(null > 0); // here null converts into a number which is 0 , and 0 is not equal to zero
// console.log(null == 0); //it is a special case where null does not convert it remains undefined so it gives false because undefined value can not be equal to 0
// console.log(null >= 0); // it is same as same as first one it can be equal so it gives true

// == loose equal // === strictly equal

console.log("2" == 2) // it gives true because it does not check datatype 

console.log("2" === 2) // it gives false value because it checks the datatype also 
